export type ContentMetaType = {
  title: string;
  description?: string;
  image?: string;
  lastEditedTime: string;
};

export type ContentHeadlineType = {
  lastEditedTime: string;
  title: string;
};
