import styled from 'styled-components'

export const CardMoreBox = styled.div`
  position: absolute;
  top: 0;
  right: 0;
`

export const CardMoreIcon = styled.div`
  padding: 8px 0;
  z-index: 1;
  position: relative;

  svg {
    display: block;
  }
`
